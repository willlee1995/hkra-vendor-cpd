# Copy to .env.edge in the repo root (gitignored).
# Used by scripts/deploy-functions-vps-password.py for password-based VPS deploy.
# Do NOT commit .env.edge.

DEPLOY_SSH_PASSWORD=
VPS_HOST=46.202.166.252
VPS_USER=root
DOCKER_CONTAINER=supabase-edge-functions
FUNCTIONS_PATH=/home/deno/functions
REMOTE_EDGE_FUNCTIONS_DIR=/tmp/hkra-edge-functions
